These bugs will not be fixed in the short term:

testex:=NewSphereMachine("a=<d^-1,d>(1,2)", "b=<a, d>", "c=<b,c>", "d=<c,c^-1>(1,2)", "a*b*c*d");
-- this is a non-flexible Lattes map, whose square is flexible (it's multiplication by sqrt(-2) on the lattice Z[sqrt(-2)]).
